Syncronizes subtractively files between master and a number of slaves.

It's assumed that files are added outside the application ( for instance using Limagito ), so the applications only responsibility is to monitor the master-folder, and remove files from slave-destinations that are removed from master.